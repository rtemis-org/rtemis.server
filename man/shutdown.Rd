% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{shutdown}
\alias{shutdown}
\title{Signal a running rtemislive server to stop}
\usage{
shutdown(server)
}
\arguments{
\item{server}{Server env returned (or shared) from
\code{\link[=serve]{serve()}}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Sets \code{server$stop_requested}. On its next tick the loop will close
the HTTP listener (which causes \code{http_server$serve()} to return) and
\code{serve()} cleans up daemons and sockets via \code{on.exit}.
}
\details{
Useful when the server runs on a mirai task or a separate R process
that can be passed the server env (e.g. in integration tests). For a
server running in the user's foreground R session, just press
Ctrl-C.
}
\examples{
\dontrun{
# Start the server on a mirai task so it doesn't block the session.
task <- mirai::mirai({
  rtemis.server::serve(port = 5757L, verbosity = 0L)
})

# ... do work ...

# Signal the server to stop gracefully.
rtemis.server::shutdown(task$data)
}
}
\author{
EDG
}
