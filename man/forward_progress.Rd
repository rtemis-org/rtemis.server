% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{forward_progress}
\alias{forward_progress}
\title{Forward a \code{rtemis::train} progress checkpoint over the msg sink}
\usage{
forward_progress(stage, current, total, message)
}
\arguments{
\item{stage}{Character scalar: Structured stage name. Becomes the
\code{caller} field on the wire envelope (e.g. \code{"outer_fold"}).}

\item{current}{Integer: 1-based index of the checkpoint. Unused by
this adapter directly (encoded into \code{message} upstream), kept in
the signature so it matches the rtemis::train \code{progress} contract.}

\item{total}{Integer: Total checkpoints. Same as \code{current} - present
to match the contract.}

\item{message}{Character scalar: Human-readable line, e.g.
\code{"Outer fold 2/5"}. Becomes the envelope's \code{text} field.}
}
\value{
Invisible \code{NULL}.
}
\description{
Thin adapter passed as \verb{progress = } to \code{rtemis::train()}. Calls
rtemis's internal \code{msg()} with \code{caller = stage}, so the daemon-side
sink (installed by \code{init_daemon_progress}) ships an envelope whose
\code{caller} field carries the structured stage name (e.g.
\code{"outer_fold"}). The host turns that into a \code{job.progress} event with
\code{data$stage} set, which the UI can route on without text-matching
the message.
}
\details{
\code{msg} is unexported from rtemis; the reference is bound at package
source-eval time in \verb{00_init.R} via \code{getFromNamespace}, so calling
\code{msg()} here avoids both \code{rtemis:::msg} (R CMD check NOTE) and any
per-call namespace lookup.

Designed to be referenced as \code{rtemis.server::forward_progress} inside
the mirai job expression - mirai loads rtemis.server on the daemon
on first use, sourcing \verb{00_init.R} once, so the \code{msg} binding exists
before the callback is ever invoked.
}
\author{
EDG
}
