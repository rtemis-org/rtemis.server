% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{ensure_daemon_sink}
\alias{ensure_daemon_sink}
\title{Lazily install the daemon-side msg sink + push socket}
\usage{
ensure_daemon_sink()
}
\value{
Invisible \code{NULL}.
}
\description{
Runs as the first action of every wrapped job expression (see
\code{submit_job} in jobs.R). On the first call per daemon, opens a
nanonext push socket dialing the URL planted by
\code{init_daemon_progress} and installs an \code{rtemis::set_msg_sink()}
that forwards every \code{msg()} call as a JSON envelope on the socket.
On subsequent calls (sink already installed), short-circuits.
}
\details{
Lives in the daemon's \code{live} env (a namespace env in rtemis) - those
writes persist across regular \code{mirai()} tasks but, critically, NOT
across \code{mirai::everywhere()} calls. See \code{init_daemon_progress} for
the full rationale.

Exported (rather than internal) so the per-job wrapped expression
in \code{submit_job} can reference it as \code{rtemis.server::ensure_daemon_sink}
without resorting to \code{:::} (CRAN-discouraged) or \code{getFromNamespace}
(extra per-job lookup). It is not part of the user-facing API.
}
\author{
EDG
}
\keyword{internal}
